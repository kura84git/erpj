CREATE TABLE usr (
user_id VARCHAR(20) PRIMARY KEY NOT NULL
, user_name VARCHAR(50) NOT NULL
, password VARCHAR(20) NOT NULL
, del_flg VARCHAR(20)
);

INSERT INTO usr
(user_id, user_name, password, del_flg)
VALUES ('kura01', 'くら', 'guest', 'kura01')
, ('admin', '管理者', 'guest', 'admin');